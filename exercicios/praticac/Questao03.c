#include <stdio.h>

int main(){ 
    float x = 2;
    float y = 3;
    float z = 0;
     
    z++;
    // z = x - y;
    // z = x + y;
    // z = x / y;
    // z = x * y;
    // z++;

    printf("%.2f\n ", z);

    return 0;
}